# TOP 5 BEST KNEE SLEEVES — Local Preview

Files created:

- index.html — main page
- styles.css — styles for the page
- assets/placeholder.svg — video placeholder image

How to preview locally:

1. Open the project folder in a simple HTTP server or your editor (VS Code Live Server). Example using Python 3:

```bash
cd /Users/sherry/tophealthtrending
python3 -m http.server 8000
# then open http://localhost:8000 in your browser
```

Notes:
- Replace `assets/placeholder.svg` with your actual video thumbnail or embed later.
